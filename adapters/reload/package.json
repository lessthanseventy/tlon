{
  "name": "@adapters/reload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "adapters/reload — restart the pi harness itself to pick up edited extension/tool code, resuming the current session (the dogfood loop's 'reload me' primitive).",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "typebox": "^1.3.14"
  },
  "pi": {
    "extensions": ["./src/extension.ts"]
  }
}
