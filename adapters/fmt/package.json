{
  "name": "@adapters/fmt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "adapters/fmt — the agent's format-on-save: mix format any .ex/.exs file the edit/write tools touch.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  },
  "pi": {
    "extensions": ["./src/extension.ts"]
  }
}