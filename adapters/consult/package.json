{
  "name": "@adapters/consult",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "adapters/consult — delegate a question to a different model: /consult (with session context) and /fresh (clean one-shot).",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "typebox": "^1.3.14"
  },
  "pi": {
    "extensions": ["./src/extension.ts"]
  }
}