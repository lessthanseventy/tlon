{
  "name": "@adapters/pi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "adapters — the pi adapter: the server's hands in the pi harness (register + brief). The dense statusline lives in the sibling adapters/footer package.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  },
  "pi": {
    "extensions": ["./src/extension.ts"]
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@earendil-works/pi-tui": "^0.84.2"
  }
}
